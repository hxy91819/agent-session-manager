interface:
  display_name: "Cross-Agent PR Review"
  short_description: "Review PRs for shared provider bug classes"
  default_prompt: "Use $cross-agent-pr-review to review this PR for the same bug class across all supported agents."
